apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}-service
  namespace: {{ k8s_namespace }}
spec:
  selector:
    app: {{ app_name }}
  ports:
    - protocol: TCP
      port: {{ port }}
      targetPort: {{ port }}
  type: LoadBalancer
